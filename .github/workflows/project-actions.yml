name: Build project
run-name: ${{ github.workflow }} by ${{ github.actor }} run build 🚀
on: [push]
jobs:
  Build-task:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          make build