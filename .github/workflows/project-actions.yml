name: Build project
run-name: Workflow «${{ github.workflow }}» by ${{ github.actor }} 🚀
on: [push]
jobs:
  Build-task:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          make build