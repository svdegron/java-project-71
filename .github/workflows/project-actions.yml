name: Build project
run-name: ${{ github.actor }} is testing actions 🍺
on: [push]
jobs:
  Build-task:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          make build